[package]
name = "gg"
version = "0.2.0"
edition = "2024"
rust-version = "1.85"
description = "像 man 一样查询你自己的命令笔记 / Query your own command notes like man"
license = "MIT"
repository = "https://github.com/wmjim/gg"
readme = "README.md"
keywords = ["cli", "notes", "man", "markdown", "cheatsheet"]
categories = ["command-line-utilities"]

[dependencies]
anyhow = "1.0.104"
clap = { version = "4.6.7", features = ["derive"] }
dirs = "7.0.0"
humantime = "2.4.0"
pulldown-cmark = "0.13.4"
serde = { version = "1.0.229", features = ["derive"] }
strsim = "0.11.1"
tempfile = "3.27.0"
toml = "1.1.6"
which = "8.0.6"

[dev-dependencies]
assert_cmd = "2.2.2"
predicates = "3.1.4"

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"

[lints.rust]
unsafe_code = "forbid"
# 未使用的导入只在特定平台出现（某分支被 cfg 掉后，导入就没人用了）。
# 提升为错误，避免它长期以 warning 形式被忽略 —— 本仓库为此踩过两次：
# 一次是删掉只在 Windows/macOS 用到的 anyhow::Context，一次是 Path/Command。
unused_imports = "deny"

[lints.clippy]
all = { level = "warn", priority = -1 }
dbg_macro = "deny"
todo = "deny"
