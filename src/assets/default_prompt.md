为命令 `{{command}}` 输出一份简易的 Markdown 速查笔记。直接输出笔记正文，不要任何前言、说明或收尾语。

必须严格采用以下结构，不要增删章节：
1. 第一行：`命令名`：一句话说明它做什么，40 字以内。
2. 提供一个 ```bash 代码块，其中写入该命令的几个高频使用示例，在其上方加一行以 # 开头的说明。
3. 用无序列表只列 2~4 个最高频的选项，每项形如 `-x`：用途。低频选项不要写。

**硬性约束**：
- 全文 10~25 行，宁少勿多。
- 命令示例必须真实可运行，不要写行尾空格。
- 输出语言：{{language}}。
